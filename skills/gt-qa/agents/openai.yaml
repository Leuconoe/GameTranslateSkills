interface:
  display_name: "Game Translation QA"
  short_description: "Integrate and verify patch runtime evidence"
  default_prompt: "Use $gt-qa to integrate text, font, and image candidates and verify structure, session ownership, build, font rendering, and runtime evidence."
policy:
  allow_implicit_invocation: true
