interface:
  display_name: "Game Patch Release"
  short_description: "Package a verified patch for distribution"
  default_prompt: "Use $gt-release to package a verified Korean game patch with installation and release notes."
policy:
  allow_implicit_invocation: true
