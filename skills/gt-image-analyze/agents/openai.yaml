interface:
  display_name: "Game Image Analysis"
  short_description: "Inventory text images, atlases, and scope"
  default_prompt: "Use $gt-image-analyze to inventory text-bearing images, atlas regions, style contracts, hashes, and image scope."
policy:
  allow_implicit_invocation: true
