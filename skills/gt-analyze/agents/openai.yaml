interface:
  display_name: "Game File Analysis"
  short_description: "Inventory files, engines, slots, and targets"
  default_prompt: "Use $gt-analyze to inspect a legally owned game project and build its extraction, engine, language-slot, font, and image inventory."
policy:
  allow_implicit_invocation: true
