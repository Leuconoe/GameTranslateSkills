interface:
  display_name: "Game Korean Localization"
  short_description: "Orchestrate branch-based Korean game localization"
  default_prompt: "Use $game-translate to start or resume the branch-based Korean game-localization pipeline, honoring review policies, font gates, image scope, session ownership, and cleanup contracts."
policy:
  allow_implicit_invocation: true
