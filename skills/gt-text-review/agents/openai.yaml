interface:
  display_name: "Text Review Handoff"
  short_description: "Prepare text review and emulator handoff"
  default_prompt: "Use $gt-text-review to prepare the complete text review sheet and emulator-ready handoff, waiting only if the project sets text_review_policy=user-gate."
policy:
  allow_implicit_invocation: true
