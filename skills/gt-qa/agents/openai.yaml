interface:
  display_name: "Game Translation QA"
  short_description: "Verify patches with runtime and emucap evidence"
  default_prompt: "Use $gt-qa to verify the Korean patch's structure, font coverage, build, and runtime behavior, optionally using emucap MCP evidence."
policy:
  allow_implicit_invocation: true
